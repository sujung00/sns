<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.sns.like.dao.LikeMapper">

	<select id="selectLikeCount" parameterType="map" resultType="int">
		SELECT
			COUNT(1)
		FROM
			`like`
		WHERE
			`postId` = #{postId}
			AND
			`userId` = #{userId}
	</select>
	
	<delete id="deleteLikeByPostIdUserId" parameterType="map">
		DELETE
		FROM `like`
		WHERE
			`postId` = #{postId}
			AND
			`userId` = #{userId}
	</delete>
	
	<insert id="insertLikeByPostIdUserId" parameterType="map">
		INSERT INTO `like`
		(
			`postId`
			,`userId`
			,`createdAt`
		)
		VALUES
		(
			#{postId}
			, #{userId}
			, NOW()
		)
	</insert>
	
	<select id="selectLikeByPostIdUserId" parameterType="map" resultType="com.sns.like.model.Like">
		SELECT
			`postId`
			,`userId`
			,`createdAt`
		FROM
			`like`
		WHERE
			`postId` = #{postId}
			AND
			`userId` = #{userId}
	</select>
	
	<select id="selectLikeCountByPostId" parameterType="int" resultType="int">
		SELECT
			COUNT(1)
		FROM
			`like`
		WHERE
			`postId` = #{postId}
	</select>

</mapper>